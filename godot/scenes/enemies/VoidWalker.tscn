[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/enemies/void_walker.gd" id="1_vw"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_vw"]
size = Vector2(16, 24)

; Void-Walker — phase-shifting crystalline entity. Intangible in Phase A (script sets alpha).
; Nested irregular crystal layers: outer shard → mid crystal → core → inner glow.
; Collision: 16×24 (half: 8×12)
; NOTE: void_walker.gd sets modulate on the root node — all children are affected.
[node name="VoidWalker" type="CharacterBody2D"]
collision_layer = 0
collision_mask = 1
script = ExtResource("1_vw")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("RectangleShape2D_vw")

; ── Outer irregular shard (jagged, asymmetric — feels unstable) ───
[node name="OuterShard" type="Polygon2D" parent="."]
polygon = PackedVector2Array(0, -12, 7, -8, 11, -2, 8, 6, 2, 12, -5, 10, -10, 2, -7, -7)
color = Color(0.20, 0.00, 0.38, 1.0)

; ── Mid crystal layer ─────────────────────────────────────────────
[node name="MidCrystal" type="Polygon2D" parent="."]
polygon = PackedVector2Array(0, -8, 6, -4, 7, 2, 3, 8, -4, 6, -7, -1)
color = Color(0.42, 0.05, 0.72, 1.0)

; ── Inner core diamond ────────────────────────────────────────────
[node name="Core" type="Polygon2D" parent="."]
polygon = PackedVector2Array(0, -5, 5, 0, 0, 6, -5, 0)
color = Color(0.65, 0.30, 1.0, 1.0)

; ── Central glow — near-white to read as energy source ───────────
[node name="InnerGlow" type="Polygon2D" parent="."]
polygon = PackedVector2Array(0, -2, 2, 0, 0, 3, -2, 0)
color = Color(0.90, 0.75, 1.0, 1.0)
