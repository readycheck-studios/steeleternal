[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/enemies/grunt.gd" id="1_grunt"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_grunt"]
size = Vector2(18, 24)

; Grunt — bipedal combat drone. Red/orange palette, amber visor.
; Collision: 18×24 (half: 9×12)
[node name="Grunt" type="CharacterBody2D"]
collision_layer = 64
collision_mask = 39
script = ExtResource("1_grunt")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("RectangleShape2D_grunt")

; ── Legs (rendered first — behind torso) ──────────────────────────
[node name="LegLeft" type="Polygon2D" parent="."]
polygon = PackedVector2Array(-8, 5, -2, 5, -2, 12, -8, 12)
color = Color(0.22, 0.12, 0.12, 1.0)

[node name="LegRight" type="Polygon2D" parent="."]
polygon = PackedVector2Array(2, 5, 8, 5, 8, 12, 2, 12)
color = Color(0.22, 0.12, 0.12, 1.0)

; ── Torso (slight trapezoid — wider at hips) ──────────────────────
[node name="Body" type="Polygon2D" parent="."]
polygon = PackedVector2Array(-8, -6, 8, -6, 9, 5, -9, 5)
color = Color(0.72, 0.15, 0.15, 1.0)

; ── Shoulder armour plates ────────────────────────────────────────
[node name="ShoulderL" type="Polygon2D" parent="."]
polygon = PackedVector2Array(-11, -8, -7, -8, -7, -3, -11, -3)
color = Color(0.85, 0.22, 0.10, 1.0)

[node name="ShoulderR" type="Polygon2D" parent="."]
polygon = PackedVector2Array(7, -8, 11, -8, 11, -3, 7, -3)
color = Color(0.85, 0.22, 0.10, 1.0)

; ── Head (tapers slightly at jaw) ─────────────────────────────────
[node name="Head" type="Polygon2D" parent="."]
polygon = PackedVector2Array(-5, -12, 5, -12, 6, -6, -6, -6)
color = Color(0.55, 0.12, 0.12, 1.0)

; ── Amber visor — horizontal eye slit ────────────────────────────
[node name="Visor" type="Polygon2D" parent="."]
polygon = PackedVector2Array(-4, -10, 4, -10, 4, -8, -4, -8)
color = Color(0.961, 0.620, 0.043, 1.0)
